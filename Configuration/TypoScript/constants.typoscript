plugin.tx_flux_elements {
    view {
        # cat=plugin.tx_flux_elements/file; type=string; label=Path to template root (FE)
        templateRootPath =
        # cat=plugin.tx_flux_elements/file; type=string; label=Path to template partials (FE)
        partialRootPath =
        # cat=plugin.tx_flux_elements/file; type=string; label=Path to template layouts (FE)
        layoutRootPath =
    }
}
